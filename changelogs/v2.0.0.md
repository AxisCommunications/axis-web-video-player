## Breaking changes

- The container element passed to `startLiveStream` must now be sized.
  The inner element created by the library will have the CSS properties `position: relative; width: 100%; heigh: 100%` and will thus grow to fill its parent.

## Fixed

- Fixed issue that could prevent the `startLiveStream` promise from ever being resolved or rejected.
- Fixed several potential memory leaks.
