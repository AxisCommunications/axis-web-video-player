## Added
- New function `vaasInit()`. This function shall be called and the returned promise shall be resolved before calling other VaaS library functions.

## Deprecation
- Automatic library initialization upon first use is deprecated. Call `vaasInit()` explicitly before using other library functions. This will also improve performance somewhat.

## Fixed
- Various race conditions which could cause broken clients in some circumstances.